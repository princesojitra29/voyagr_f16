// src/PromptPage.tsx

// Import the PromptForm component, which contains the input form and submit logic
import PromptForm from "./PromptForm";

// Functional component for the Prompt Page
export default function PromptPage() {
  // Render the PromptForm component
  // This page essentially serves as a wrapper for the form
  return <PromptForm />;
}
